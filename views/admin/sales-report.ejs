<%- include('./layouts/header.ejs') -%> <%- include('./layouts/skins.ejs') -%>
<%- include('./layouts/sidebar.ejs') -%>

<div class="main-panel">
  <div class="content-wrapper">
    <div class="row">
      <div class="col-md-12 grid-margin stretch-card">
        <div class="card">
          <div class="card-body">
            <p class="card-title">Advanced Table</p>
            <div class="row">
              <div class="col-12">
                <div class="table-responsive">
                  <table class="display expandable-table" style="width: 100%">
                    <thead>
                      <tr>
                        <th>custommer</th>
                        <th>Product</th>
                        <th>quantity</th>
                        <th>Amount</th>
                        <th>Payment Method</th>
                        <th>Payment</th>
                        <th>Date</th>
                        <th>Order Status</th>
                        <th>Print</th>
                      </tr>
                    </thead>
                    <tbody>
                      <% for(let i=1; i<=30; i++){ %> <%
                      orders.forEach((order)={ %> <% if (
                      moment(order.date).format('d') == i ){ %> <% let
                      someOfTotal =
                      order.grandTotal.reduce((toatl,value)=>total+value),0 %>
                      <% } %>
                      <tr>
                        <td><%= moment(orders.date).format('LL') %></td>
                        <td><%= someOfTotal %></td>
                      </tr>
                      <% }) %> <% } %>
                    </tbody>
                  </table>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
<script>
  function changeStatus(status, orderId, proId) {
    axios.post("/admin/order-manage", { status, orderId, proId }).then(() => {
      console.log("evidim");
      window.location.reload();
    });
  }
</script>

<%- include('./layouts/footer.ejs') -%>
